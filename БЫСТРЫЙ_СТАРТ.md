# ⚡ Быстрый запуск через GitHub

Самый простой способ **бесплатно** развернуть Saqta TWA в продакшн.

---

## 🎯 Что получится

- ✅ **Фронтенд**: Бесплатный хостинг на Vercel (безлимит)
- ✅ **Бэкенд**: Бесплатный хостинг на Railway ($5/месяц кредитов) или Render (750 часов/месяц)
- ✅ **База данных**: Бесплатная PostgreSQL
- ✅ **Автодеплой**: При каждом push в GitHub автоматически обновляется!

**Время настройки**: ~10 минут

---

## 📋 Быстрые шаги

### 1️⃣ Загрузите код на GitHub (1 мин)

```bash
# Перейдите в папку проекта
cd /Users/aidin/TWA-Saqta

# Инициализируйте Git (если еще не сделано)
git init
git add .
git commit -m "Initial commit"

# Создайте репозиторий на GitHub.com
# Затем подключите и загрузите:
git remote add origin https://github.com/ваш-username/TWA-Saqta.git
git branch -M main
git push -u origin main
```

✅ Код теперь на GitHub!

---

### 2️⃣ Деплой бэкенда на Railway (3 мин)

1. **Откройте**: [railway.app](https://railway.app)
2. **Войдите**: "Login" → "Continue with GitHub"
3. **Создайте проект**: "New Project" → "Deploy from GitHub repo" → выберите `TWA-Saqta`
4. **Добавьте БД**: "New" → "Database" → "PostgreSQL" (подождите 30 сек)
5. **Настройте сервис**:
   - Откройте web service → "Settings"
   - **Root Directory**: `server`
   - Перейдите в "Variables" → добавьте:
     ```
     DATABASE_URL → (скопируйте из PostgreSQL сервиса)
     NODE_ENV → production
     TELEGRAM_BOT_TOKEN → (получите у @BotFather: /newbot)
     FRONTEND_URL → https://twa-saqta.vercel.app (обновим позже)
     PORT → 3001
     ```
6. **Получите URL**: "Settings" → "Networking" → "Generate Domain"
   - 📋 **Скопируйте URL** (например: `https://twa-saqta-production.up.railway.app`)

✅ Бэкенд работает!

---

### 3️⃣ Деплой фронтенда на Vercel (2 мин)

1. **Откройте**: [vercel.com](https://vercel.com)
2. **Войдите**: "Sign Up" → "Continue with GitHub"
3. **Импортируйте**: "Add New..." → "Project" → выберите `TWA-Saqta`
4. **Настройте**:
   - Framework Preset: **Vite**
   - Root Directory: `./` (по умолчанию)
   - Build Command: `npm run build`
   - Output Directory: `dist`
   - **Environment Variables**:
     ```
     VITE_API_URL → https://ваш-backend.up.railway.app/api
     ```
     ⚠️ Используйте URL из шага 2, и добавьте `/api` в конце!
5. **Deploy** → дождитесь ~2 минуты
6. 📋 **Скопируйте Production URL** (например: `https://twa-saqta.vercel.app`)

✅ Фронтенд онлайн!

---

### 4️⃣ Обновите настройки (2 мин)

#### A. Обновите Railway
1. Откройте Railway → ваш backend service → "Variables"
2. Измените `FRONTEND_URL` на URL из Vercel (шаг 3)
3. Сервис автоматически перезапустится

#### B. Настройте Telegram бота
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте: `/deleteapp` → удалите старый (если был)
3. Отправьте: `/newapp` → выберите вашего бота
4. Заполните:
   - **Title**: Saqta
   - **Description**: Food surplus marketplace
   - **Photo**: `/empty` (пока без фото)
   - **Demo GIF**: `/empty`
   - **Web App URL**: `https://twa-saqta.vercel.app` (URL из шага 3)
   - **Short name**: `saqta`

BotFather даст ссылку: `https://t.me/ваш_бот/saqta`

✅ **Готово!** 🎉

---

### 5️⃣ Проверьте работу (1 мин)

1. **Откройте** ссылку от BotFather в Telegram
2. **Проверьте**:
   - ✅ TWA загружается
   - ✅ Можно просмотреть офферы (если есть тестовые данные)
   - ✅ Переключение на Merchant Panel работает

Если что-то не работает → см. [Решение проблем](#-решение-проблем)

---

## 🔄 Как обновлять приложение

Теперь каждый раз, когда вы делаете изменения:

```bash
# Сделайте изменения в коде
# Затем:
git add .
git commit -m "Описание изменений"
git push
```

**И всё!** Railway и Vercel автоматически:
- 📦 Соберут новую версию
- 🚀 Задеплоят её
- ✅ Обновят приложение (~2 минуты)

---

## 🆓 Альтернатива: Render (100% бесплатно)

Если хотите полностью бесплатный вариант (но медленнее):

### Вместо Railway используйте Render:

1. **Откройте**: [render.com](https://render.com)
2. **Войдите**: "Sign Up" → "GitHub"
3. **Создайте PostgreSQL**:
   - "New" → "PostgreSQL"
   - Name: `saqta-db`
   - Plan: **Free**
   - 📋 Скопируйте **Internal Database URL**
4. **Создайте Web Service**:
   - "New" → "Web Service"
   - Подключите репозиторий `TWA-Saqta`
   - Settings:
     - Name: `saqta-backend`
     - **Root Directory**: `server`
     - **Build Command**: `npm install && npx prisma generate && npm run build`
     - **Start Command**: `npx prisma migrate deploy && node dist/index.js`
     - Plan: **Free**
   - **Environment**:
     ```
     DATABASE_URL → (из шага 3)
     NODE_ENV → production
     TELEGRAM_BOT_TOKEN → ваш_токен
     FRONTEND_URL → https://twa-saqta.vercel.app
     PORT → 3001
     ```
5. 📋 Скопируйте URL (например: `https://saqta-backend.onrender.com`)

Затем продолжайте с шага 3 (Vercel).

⚠️ **Важно**: Render Free план "засыпает" после 15 мин неактивности. Первый запрос после пробуждения займет ~30 секунд.

---

## 🐛 Решение проблем

### "CORS error"
- ✅ Проверьте: `FRONTEND_URL` в Railway **точно совпадает** с URL Vercel
- ✅ Перезапустите backend service на Railway

### "Database connection failed"
- ✅ Проверьте: `DATABASE_URL` правильно скопирован из PostgreSQL сервиса
- ✅ В Railway: убедитесь, что PostgreSQL сервис запущен (зеленая галочка)

### TWA не открывается
- ✅ URL в BotFather должен быть **HTTPS** (не HTTP!)
- ✅ URL должен **точно совпадать** с Production URL в Vercel
- ✅ Попробуйте `/deleteapp` и создать заново

### "Build failed" на Vercel
- ✅ Проверьте логи деплоя в Vercel Dashboard
- ✅ Убедитесь, что `VITE_API_URL` правильно установлен
- ✅ Попробуйте собрать локально: `npm run build`

### "Build failed" на Railway
- ✅ Проверьте: Root Directory = `server`
- ✅ Проверьте логи в Railway
- ✅ Убедитесь, что в `server/package.json` есть скрипт `build`

---

## 📊 Мониторинг

### Проверка логов

**Vercel**:
Dashboard → Project → Deployments → View Logs

**Railway**:
Dashboard → Service → Deployments → View Logs

**Render**:
Dashboard → Service → Logs

### Проверка БД

**Railway**:
Dashboard → PostgreSQL → Data → Query

**Render**:
Dashboard → PostgreSQL → Connect → используйте любой PostgreSQL клиент

---

## 💰 Бесплатные лимиты

### Railway (рекомендуется)
- 💵 **$5** кредитов в месяц
- ⏱️ ~**500 часов** работы
- 💾 **1GB** хранилище БД
- ✅ **Не засыпает**

### Render (100% бесплатно)
- ⏱️ **750 часов** в месяц
- 💾 **1GB** хранилище БД
- ⚠️ **Засыпает** после 15 мин
- ⏰ **Медленный старт** (~30 сек)

### Vercel
- ✅ **Безлимит** для некоммерческих проектов
- 🚀 **Быстрый CDN**
- 🔄 **Автодеплой** из GitHub

---

## ✅ Проверочный чеклист

- [ ] Код загружен на GitHub
- [ ] Railway/Render аккаунт создан
- [ ] PostgreSQL база создана
- [ ] Бэкенд задеплоен
- [ ] Backend URL скопирован
- [ ] Vercel аккаунт создан
- [ ] Фронтенд задеплоен
- [ ] Frontend URL скопирован
- [ ] Переменные окружения установлены (обе стороны!)
- [ ] `FRONTEND_URL` обновлен в Railway
- [ ] BotFather настроен с новым URL
- [ ] TWA протестирован в Telegram

---

## 📚 Дополнительно

- 📖 [Полная инструкция по запуску (русский)](./ЗАПУСК.md)
- 📖 [Документация проекта (англ.)](./README.md)
- 🚀 [Детальный гайд по деплою (англ.)](./DEPLOYMENT.md)

---

**Вопросы?** Создайте Issue на GitHub или проверьте [полную инструкцию](./ЗАПУСК.md).

**Готово! Ваш TWA теперь доступен всему миру! 🌍✨**
