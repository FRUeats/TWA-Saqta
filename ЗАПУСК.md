# 🚀 Инструкция по запуску Saqta

Полное руководство по запуску проекта Saqta (Telegram WebApp для продажи излишков еды).

---

## 📋 Содержание

1. [Локальный запуск для разработки](#-локальный-запуск-для-разработки)
2. [Бесплатный деплой в продакшн](#-бесплатный-деплой-в-продакшн)
3. [Решение проблем](#-решение-проблем)

---

## 💻 Локальный запуск для разработки

### Шаг 1: Требования

Убедитесь, что установлены:
- ✅ **Node.js** версии 18 или выше ([скачать](https://nodejs.org/))
- ✅ **PostgreSQL** база данных ([скачать](https://www.postgresql.org/download/))
- ✅ **Git** для клонирования репозитория
- ✅ **Telegram Bot Token** от [@BotFather](https://t.me/BotFather)

### Шаг 2: Клонирование и установка зависимостей

```bash
# Перейдите в папку проекта
cd /Users/aidin/TWA-Saqta

# Установите зависимости для фронтенда
npm install

# Установите зависимости для бэкенда
cd server
npm install
cd ..
```

### Шаг 3: Настройка базы данных PostgreSQL

#### Вариант A: Локальная установка PostgreSQL

1. **Установите PostgreSQL** (если еще не установлен):
   ```bash
   # macOS (через Homebrew)
   brew install postgresql@15
   brew services start postgresql@15
   ```

2. **Создайте базу данных**:
   ```bash
   # Войдите в PostgreSQL
   psql postgres
   
   # Создайте пользователя и базу данных
   CREATE USER saqta_user WITH PASSWORD 'your_password';
   CREATE DATABASE saqta_dev;
   GRANT ALL PRIVILEGES ON DATABASE saqta_dev TO saqta_user;
   \q
   ```

#### Вариант B: Бесплатная облачная база данных (рекомендуется для тестов)

**Supabase** - бесплатно до 500MB:
1. Зарегистрируйтесь на [supabase.com](https://supabase.com)
2. Создайте новый проект
3. Скопируйте **Database URL** из Settings → Database → Connection String → URI

**Neon** - бесплатно до 3GB:
1. Зарегистрируйтесь на [neon.tech](https://neon.tech)
2. Создайте новый проект
3. Скопируйте строку подключения

### Шаг 4: Настройка переменных окружения

#### 4.1 Для бэкенда

Создайте файл `server/.env`:

```bash
cd server
cp .env.example .env
```

Откройте `server/.env` и заполните:

```env
# База данных (вставьте вашу строку подключения)
DATABASE_URL="postgresql://saqta_user:your_password@localhost:5432/saqta_dev?schema=public"

# Порт сервера
PORT=3001
NODE_ENV=development

# Telegram Bot Token (получите у @BotFather)
TELEGRAM_BOT_TOKEN=your_bot_token_here

# URL фронтенда (для CORS)
FRONTEND_URL=http://localhost:5173
```

#### 4.2 Для фронтенда

Создайте файл `.env.local` в корне проекта:

```bash
cd /Users/aidin/TWA-Saqta
cp .env.example .env.local
```

Откройте `.env.local` и заполните:

```env
# URL бэкенда
VITE_API_URL=http://localhost:3001/api
```

### Шаг 5: Инициализация базы данных

```bash
cd server

# Создайте таблицы в базе данных
npx prisma migrate dev --name init

# (Опционально) Заполните тестовыми данными
npm run seed

# Откройте Prisma Studio для просмотра данных
npx prisma studio
```

Prisma Studio откроется в браузере на `http://localhost:5555` - здесь можно просматривать и редактировать данные.

### Шаг 6: Запуск приложения

Вам нужны **2 терминала**:

#### Терминал 1 - Бэкенд:
```bash
cd /Users/aidin/TWA-Saqta/server
npm run dev
```

✅ Сервер запустится на `http://localhost:3001`

#### Терминал 2 - Фронтенд:
```bash
cd /Users/aidin/TWA-Saqta
npm run dev
```

✅ Приложение откроется на `http://localhost:5173`

### Шаг 7: Тестирование в Telegram

Для тестирования TWA локально нужно использовать **ngrok** для публичного URL:

```bash
# Установите ngrok
brew install ngrok

# Или скачайте с https://ngrok.com

# Запустите туннель для фронтенда
ngrok http 5173
```

Скопируйте HTTPS URL (например, `https://abc123.ngrok.io`) и:

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/newapp` или `/editapp`
3. Выберите ваш бот
4. Установите **Web App URL**: `https://abc123.ngrok.io`

Теперь можно открыть TWA в Telegram!

---

## ☁️ Бесплатный деплой в продакшн

### Вариант 1: Railway + Vercel (Рекомендуется)

#### ✅ **Что бесплатно:**
- **Railway**: $5 кредитов в месяц (достаточно для тестового бэкенда + БД)
- **Vercel**: Безлимитный хостинг фронтенда

#### Шаг 1: Загрузите код на GitHub

```bash
# Если еще не создали репозиторий:
cd /Users/aidin/TWA-Saqta
git init
git add .
git commit -m "Initial commit"

# Создайте новый репозиторий на github.com
# Затем:
git remote add origin https://github.com/ваш-username/TWA-Saqta.git
git branch -M main
git push -u origin main
```

#### Шаг 2: Деплой бэкенда на Railway

1. **Зарегистрируйтесь**: [railway.app](https://railway.app) → Login with GitHub
2. **Создайте проект**: New Project → Deploy from GitHub repo → выберите `TWA-Saqta`
3. **Добавьте PostgreSQL**: New → Database → PostgreSQL
4. **Настройте переменные окружения**:
   - Откройте ваш web service → Settings
   - **Root Directory**: `server`
   - Variables → добавьте:
     ```
     DATABASE_URL (скопируйте из PostgreSQL сервиса)
     NODE_ENV=production
     TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
     FRONTEND_URL=https://ваш-app.vercel.app (добавим позже)
     PORT=3001
     ```
5. **Получите URL**: Settings → Networking → Generate Domain
   - Скопируйте URL (например, `https://twa-saqta-production.up.railway.app`)

#### Шаг 3: Деплой фронтенда на Vercel

1. **Зарегистрируйтесь**: [vercel.com](https://vercel.com) → Sign Up with GitHub
2. **Импортируйте проект**: Add New → Project → Import `TWA-Saqta`
3. **Настройки сборки**:
   - Framework Preset: **Vite**
   - Root Directory: `./` (оставьте корень)
   - Build Command: `npm run build`
   - Output Directory: `dist`
4. **Переменные окружения**: Environment Variables → добавьте:
   ```
   VITE_API_URL=https://ваш-backend.up.railway.app/api
   ```
   (используйте URL из Railway, Шаг 2)
5. **Deploy** → дождитесь завершения
6. **Скопируйте URL** (например, `https://twa-saqta.vercel.app`)

#### Шаг 4: Обновите настройки

1. **Вернитесь в Railway** → откройте backend service → Variables → обновите:
   ```
   FRONTEND_URL=https://twa-saqta.vercel.app
   ```
2. **Откройте [@BotFather](https://t.me/BotFather)**:
   - `/deleteapp` → удалите старое приложение
   - `/newapp` → создайте новое:
     - Title: **Saqta**
     - Description: **Food surplus marketplace**
     - Photo: `/empty`
     - Web App URL: `https://twa-saqta.vercel.app`
     - Short name: `saqta`

✅ **Готово!** Ваше приложение доступно по ссылке: `https://t.me/ваш_бот/saqta`

---

### Вариант 2: Render (100% бесплатно)

#### ✅ **Что бесплатно:**
- **Render**: Бесплатный план для веб-сервисов + PostgreSQL (750 часов/месяц)
- **Vercel**: Фронтенд (как выше)

#### Деплой на Render:

1. **Зарегистрируйтесь**: [render.com](https://render.com) → Sign Up with GitHub
2. **Создайте PostgreSQL**:
   - New → PostgreSQL
   - Name: `saqta-db`
   - Plan: **Free**
   - Скопируйте **Internal Database URL**
3. **Создайте Web Service**:
   - New → Web Service
   - Connect Repository: `TWA-Saqta`
   - Settings:
     - Name: `saqta-backend`
     - Root Directory: `server`
     - Build Command: `npm install && npx prisma generate && npm run build`
     - Start Command: `npx prisma migrate deploy && node dist/index.js`
     - Plan: **Free**
   - Environment Variables:
     ```
     DATABASE_URL (из шага 2)
     NODE_ENV=production
     TELEGRAM_BOT_TOKEN=ваш_токен
     FRONTEND_URL=https://ваш-app.vercel.app
     PORT=3001
     ```
4. **Скопируйте URL** (например, `https://saqta-backend.onrender.com`)

Затем продолжайте с Vercel как в Варианте 1.

⚠️ **Importante**: Бесплатный план Render "засыпает" после 15 минут неактивности. Первый запрос будет медленным (~30 сек).

---

### Вариант 3: GitHub Pages + Бесплатный бэкенд

#### Фронтенд на GitHub Pages:

```bash
# Установите gh-pages
npm install --save-dev gh-pages

# Добавьте в package.json scripts:
"predeploy": "npm run build",
"deploy": "gh-pages -d dist"

# Деплой
npm run deploy
```

Фронтенд будет доступен: `https://ваш-username.github.io/TWA-Saqta/`

⚠️ **Важно**: Вам все равно нужен бэкенд (используйте Railway/Render).

---

## 🐛 Решение проблем

### Проблема: "Cannot find module 'express'"
```bash
cd server
rm -rf node_modules package-lock.json
npm install
```

### Проблема: "Database connection failed"
- Проверьте правильность `DATABASE_URL` в `.env`
- Убедитесь, что PostgreSQL запущен: `brew services list`
- Проверьте пароль и имя пользователя

### Проблема: "CORS error" при локальной разработке
- Убедитесь, что `FRONTEND_URL` в `server/.env` = `http://localhost:5173`
- Перезапустите бэкенд

### Проблема: "Prisma Client is not generated"
```bash
cd server
npx prisma generate
```

### Проблема: TWA не открывается в Telegram
- Проверьте, что URL в BotFather **точно совпадает** с URL фронтенда
- URL должен быть **HTTPS** (для продакшн)
- Для локальной разработки используйте **ngrok**

### Проблема: "Port 3001 already in use"
```bash
# Найдите процесс
lsof -i :3001

# Убейте процесс
kill -9 PID
```

---

## 📝 Быстрые команды

### Разработка:
```bash
# Запуск бэкенда
cd server && npm run dev

# Запуск фронтенда (в отдельном терминале)
npm run dev

# Просмотр БД
cd server && npx prisma studio
```

### Деплой обновлений:
```bash
# Закоммитьте изменения
git add .
git commit -m "Описание изменений"
git push

# Railway и Vercel автоматически задеплоят! ✨
```

### Миграции БД:
```bash
# Локально
cd server
npx prisma migrate dev --name описание_изменения

# На продакшн (Railway)
railway run npx prisma migrate deploy
```

---

## 🎯 Проверочный чеклист

### Локальный запуск:
- [ ] PostgreSQL установлен и запущен
- [ ] Создана база данных `saqta_dev`
- [ ] Файлы `.env` настроены (фронтенд и бэкенд)
- [ ] Зависимости установлены (`npm install` в корне и в `server/`)
- [ ] Миграции выполнены (`npx prisma migrate dev`)
- [ ] Бэкенд запущен на `http://localhost:3001`
- [ ] Фронтенд запущен на `http://localhost:5173`
- [ ] Данные видны в Prisma Studio

### Продакшн деплой:
- [ ] Код загружен на GitHub
- [ ] Railway аккаунт создан
- [ ] PostgreSQL добавлена на Railway
- [ ] Бэкенд задеплоен на Railway
- [ ] Backend URL скопирован
- [ ] Vercel аккаунт создан
- [ ] Фронтенд задеплоен на Vercel
- [ ] Frontend URL скопирован
- [ ] Все переменные окружения установлены
- [ ] BotFather обновлен с новым URL
- [ ] TWA протестирован в Telegram

---

## 🔗 Полезные ссылки

- 📖 [Подробная документация](./README.md)
- 🚀 [Гайд по деплою (англ.)](./DEPLOYMENT.md)
- 🤖 [Telegram Bot API](https://core.telegram.org/bots/webapps)
- 🛠 [Prisma Docs](https://www.prisma.io/docs)

---

**Успешного запуска! 🎉**

Если возникнут вопросы - проверьте раздел "Решение проблем" или создайте Issue на GitHub.
