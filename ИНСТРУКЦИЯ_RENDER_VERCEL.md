# 🚀 Инструкция: Деплой связки Render + Vercel

Это пошаговое руководство, как **бесплатно** развернуть Saqta TWA, используя:
- **Render** для Бэкенда и Базы Данных (100% бесплатно)
- **Vercel** для Фронтенда (Бесплатно и быстро)

---

## 📋 Шаг 0: Подготовка

Убедитесь, что ваш код загружен на GitHub:
👉 [https://github.com/FRUeats/TWA-Saqta](https://github.com/FRUeats/TWA-Saqta)

---

## ☁️ Шаг 1: Настройка Бэкенда на Render

### 1.1 Создайте базу данных (PostgreSQL)
1. Зайдите на [dashboard.render.com](https://dashboard.render.com)
2. Нажмите **New +** → **PostgreSQL**
3. Заполните:
   - **Name**: `saqta-db`
   - **User**: (оставьте как есть)
   - **Region**: Frankfurt (или любой ближе к вам)
   - **PostgreSQL Version**: 15 (или 16)
   - **Instance Type**: **Free**
4. Нажмите **Create Database**
5. ⏳ Подождите пару минут, пока статус не станет `Available`
6. Скопируйте **Internal Database URL** (пригодится для бэкенда на Render)
7. Скопируйте **External Database URL** (если хотите подключиться с компьютера)

### 1.2 Создайте веб-сервис (Backend)
1. Нажмите **New +** → **Web Service**
2. Выберите **Build and deploy from a Git repository**
3. Подключите ваш репозиторий `FRUeats/TWA-Saqta`
4. Заполните настройки:
   - **Name**: `saqta-backend`
   - **Region**: Тот же, что и у БД (например, Frankfurt)
   - **Branch**: `main`
   - **Root Directory**: `server` (⚠️ Важно!)
   - **Runtime**: Node
   - **Build Command**: `npm install && npx prisma generate && npm run build`
   - **Start Command**: `npx prisma migrate deploy && node dist/index.js`
   - **Instance Type**: **Free**
5. Прокрутите вниз до **Environment Variables** и добавьте:
   - `DATABASE_URL` = Вставьте **Internal Database URL** (из шага 1.1)
   - `NODE_ENV` = `production`
   - `TELEGRAM_BOT_TOKEN` = Ваш токен от @BotFather
   - `PORT` = `3001`
   - `FRONTEND_URL` = `https://twa-saqta.vercel.app` (пока укажите так, позже обновим)
6. Нажмите **Create Web Service**

⏳ Подождите, пока деплой завершится (статус `Live`).
📋 **Скопируйте URL** вашего бэкенда (например: `https://saqta-backend.onrender.com`) - он вверху слева.

⚠️ **Важно про Render Free**:
- Бесплатный сервер "засыпает" через 15 минут бездействия.
- Первый запрос после сна может занять **~30-50 секунд**.
- БД не удаляется, она персистентная.

---

## ⚡ Шаг 2: Настройка Фронтенда на Vercel

### 2.1 Импорт проекта
1. Зайдите на [vercel.com](https://vercel.com)
2. Нажмите **Add New...** → **Project**
3. Нажмите **Import** напротив `TWA-Saqta`
4. Настройте "Build and Output Settings":
   - **Framework Preset**: Vite
   - **Root Directory**: `./` (оставьте пустым)
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
5. Раскройте **Environment Variables**:
   - Имя: `VITE_API_URL`
   - Значение: `https://saqta-backend.onrender.com/api` (URL из Render + `/api` в конце)
6. Нажмите **Deploy**

⏳ Подождите ~1 минуту.
🎉 Появится экран "Congratulations!".
📋 **Скопируйте домен** (например: `https://twa-saqta.vercel.app`).

---

## 🔄 Шаг 3: Связываем все вместе

### 3.1 Обновите Бэкенд
1. Вернитесь в **Render** → Dashboard → `saqta-backend`
2. Зайдите в меню **Environment**
3. Найдите `FRONTEND_URL` и нажмите **Edit**
4. Вставьте реальный домен Vercel (например: `https://twa-saqta.vercel.app`)
5. Нажмите **Save Changes**
6. Render автоматически начнет новый деплой.

### 3.2 Настройте Telegram Бота
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/mybots` → выберите вашего бота
3. Нажмите **Bot Settings** → **Menu Button** → **Configure Menu Button**
4. Отправьте ссылку на Vercel: `https://twa-saqta.vercel.app`
5. (Опционально) Обновите и описание: `/setdescription`

---

## ✅ Готово! Как проверить?

1. Откройте вашего бота в Telegram
2. Нажмите кнопку меню (появится TWA)
3. **Первый запуск будет долгим** (~30 сек), потому что Render "просыпается".
   - Вы увидите лоадер или белый экран.
   - Просто подождите.
   - Последующие запуски будут быстрыми.

---

## 🛠 Полезные советы

### Как смотреть логи (если что-то сломалось)
- **Render**: Dashboard → ваш сервис → вкладка **Logs**. Тут видно, стартовал ли сервер и ошибки БД.
- **Vercel**: Dashboard → ваш проект → вкладка **Logs**. Тут ошибки сборки фронтенда.

### Как обновить код
Просто сделайте `git push` в GitHub.
- Render увидит изменения в папке `server` и пересоберет бэкенд.
- Vercel увидит изменения и пересоберет фронтенд.

### База данных
Чтобы посмотреть данные в базе на Render:
1. Зайдите в PostgreSQL сервис на Render
2. Нажмите вкладку **Connect**
3. Используйте **External Database URL**, чтобы подключиться через программу (например, TablePlus или DBeaver) с вашего компьютера.

---
**Наслаждайтесь вашим бесплатным продакшном!** 🚀
