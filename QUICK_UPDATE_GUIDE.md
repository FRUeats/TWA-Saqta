# ⚡ Быстрое обновление деплоя

## ✅ GitHub - Готово!

Код уже обновлен в GitHub:
- ✅ Все изменения запушены
- ✅ Commit: `8657b93`

---

## 🚀 Render (Backend) - Обновление

### Вариант 1: Автоматический деплой (если настроен)

Render автоматически обнаружит новый коммит и начнет деплой. Просто подождите 2-3 минуты.

### Вариант 2: Ручной деплой

1. Откройте https://dashboard.render.com
2. Выберите ваш Backend сервис
3. Нажмите **"Manual Deploy"** → **"Deploy latest commit"**

### ⚠️ Важно: Проверьте переменные окружения

В Render Dashboard → Environment Variables убедитесь, что есть:

```env
ALLOWED_ORIGINS=https://ваш-проект.vercel.app
```

Если этой переменной нет - добавьте её! Это нужно для CORS.

---

## 🌐 Vercel (Frontend) - Обновление

### Вариант 1: Автоматический деплой (если настроен)

Vercel автоматически обнаружит новый коммит и начнет деплой. Подождите 1-2 минуты.

### Вариант 2: Ручной деплой

1. Откройте https://vercel.com/dashboard
2. Выберите ваш проект
3. Перейдите в **"Deployments"**
4. Нажмите **"Redeploy"** → выберите последний коммит

---

## 🔍 Проверка после деплоя

### Backend (Render)

1. Откройте: `https://ваш-бэкенд.onrender.com/health`
   - Должен вернуть: `{"status":"ok","timestamp":"..."}`

2. Проверьте логи в Render Dashboard
   - Не должно быть ошибок про `zod` (он уже в package.json)

### Frontend (Vercel)

1. Откройте ваше приложение
2. Проверьте, что:
   - ✅ Онбординг показывается новым пользователям
   - ✅ Формы работают (валидация)
   - ✅ Нет ошибок в консоли браузера

---

## 🐛 Если что-то пошло не так

### Render: Build fails

**Проблема**: Ошибка "Cannot find module 'zod'"

**Решение**: 
- Убедитесь, что в `server/package.json` есть `"zod": "^4.3.6"`
- Перезапустите деплой

### CORS ошибки

**Проблема**: CORS ошибки в браузере

**Решение**:
1. Проверьте `ALLOWED_ORIGINS` в Render
2. Должен быть: `https://ваш-проект.vercel.app` (без слеша!)
3. Перезапустите сервис в Render

### Vercel: API не работает

**Проблема**: Фронтенд не может подключиться к бэкенду

**Решение**:
1. Проверьте `VITE_API_URL` в Vercel
2. Должен быть: `https://ваш-бэкенд.onrender.com/api`
3. Убедитесь, что бэкенд запущен в Render

---

## 📋 Чеклист

- [x] Код запушен в GitHub ✅
- [ ] Render: Проверен/добавлен `ALLOWED_ORIGINS`
- [ ] Render: Запущен деплой (автоматически или вручную)
- [ ] Vercel: Запущен деплой (автоматически или вручную)
- [ ] Проверен health check бэкенда
- [ ] Проверена работа фронтенда
- [ ] Проверен онбординг

---

## 📚 Подробные инструкции

- **Полная инструкция по обновлению**: `DEPLOYMENT_UPDATE.md`
- **Инструкция по деплою на Render**: `RENDER_DEPLOY.md`

---

## 🎉 Готово!

После выполнения всех шагов ваше приложение будет обновлено!
