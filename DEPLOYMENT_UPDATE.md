# 🚀 Инструкция по обновлению деплоя

## ✅ GitHub - Обновлено

Код успешно запушен в GitHub:
- Commit: `1ccb611`
- Branch: `main`
- Все изменения включены

---

## 📦 Render (Backend) - Инструкция по обновлению

### Автоматический деплой (если настроен)

Если у вас настроен автоматический деплой из GitHub:
1. Render автоматически обнаружит новый коммит
2. Начнется процесс сборки
3. Деплой произойдет автоматически

### Ручной деплой

Если нужно обновить вручную:

1. **Зайдите в Render Dashboard**
   - Откройте ваш сервис (Backend)

2. **Нажмите "Manual Deploy" → "Deploy latest commit"**

3. **Проверьте переменные окружения**

   Убедитесь, что в Render настроены следующие переменные:
   ```
   DATABASE_URL=postgresql://... (ваша база данных)
   PORT=3001 (или другой порт)
   NODE_ENV=production
   TELEGRAM_BOT_TOKEN=ваш_токен_бота
   ALLOWED_ORIGINS=https://ваш-фронтенд.vercel.app,https://ваш-фронтенд.vercel.app
   ```

4. **Проверьте Build Command**
   ```
   cd server && npm install && npx prisma generate && npm run build
   ```

5. **Проверьте Start Command**
   ```
   cd server && npx prisma migrate deploy && node dist/index.js
   ```

### Важно для Render:

- ✅ Убедитесь, что `zod` установлен (добавлен в `package.json`)
- ✅ Проверьте, что Prisma миграции применены
- ✅ Настройте `ALLOWED_ORIGINS` для CORS (ваш Vercel URL)

---

## 🌐 Vercel (Frontend) - Инструкция по обновлению

### Автоматический деплой (если настроен)

Если у вас настроен автоматический деплой из GitHub:
1. Vercel автоматически обнаружит новый коммит
2. Начнется процесс сборки
3. Деплой произойдет автоматически

### Ручной деплой

1. **Зайдите в Vercel Dashboard**
   - Откройте ваш проект

2. **Нажмите "Deployments" → "Redeploy" → выберите последний коммит**

3. **Проверьте переменные окружения**

   Убедитесь, что в Vercel настроены:
   ```
   VITE_API_URL=https://ваш-бэкенд.onrender.com/api
   ```

4. **Проверьте Build Settings**
   - Framework: Vite
   - Build Command: `npm run build`
   - Output Directory: `dist`
   - Install Command: `npm install`

### После деплоя:

- ✅ Проверьте, что фронтенд подключается к бэкенду
- ✅ Проверьте, что онбординг показывается новым пользователям
- ✅ Проверьте работу валидации форм

---

## 🔧 Настройка переменных окружения

### Render (Backend)

В Render Dashboard → Environment Variables добавьте:

```env
DATABASE_URL=postgresql://... (ваша база)
PORT=3001
NODE_ENV=production
TELEGRAM_BOT_TOKEN=ваш_токен
ALLOWED_ORIGINS=https://ваш-проект.vercel.app
```

**Важно**: `ALLOWED_ORIGINS` должен содержать URL вашего Vercel фронтенда (через запятую, если несколько).

### Vercel (Frontend)

В Vercel Dashboard → Settings → Environment Variables:

```env
VITE_API_URL=https://ваш-бэкенд.onrender.com/api
```

---

## 🧪 Проверка после деплоя

### Backend (Render)

1. Проверьте health check:
   ```
   GET https://ваш-бэкенд.onrender.com/health
   ```
   Должен вернуть: `{"status":"ok","timestamp":"..."}`

2. Проверьте CORS:
   - Откройте DevTools в браузере
   - Проверьте, что запросы к API проходят без CORS ошибок

### Frontend (Vercel)

1. Откройте приложение
2. Проверьте, что онбординг показывается новым пользователям
3. Проверьте работу форм (валидация должна работать)
4. Проверьте подключение к API

---

## 🐛 Возможные проблемы

### Render: Build fails

**Проблема**: Ошибка при установке зависимостей

**Решение**:
- Убедитесь, что `zod` добавлен в `package.json` (уже добавлен)
- Проверьте версию Node.js в Render (должна быть 18+)

### Render: Prisma errors

**Проблема**: Ошибки Prisma при деплое

**Решение**:
- Убедитесь, что `prisma migrate deploy` выполняется в Start Command
- Проверьте `DATABASE_URL` в переменных окружения

### Vercel: API не работает

**Проблема**: Фронтенд не может подключиться к бэкенду

**Решение**:
- Проверьте `VITE_API_URL` в Vercel
- Проверьте `ALLOWED_ORIGINS` в Render (должен содержать Vercel URL)
- Проверьте, что бэкенд запущен и доступен

### CORS ошибки

**Проблема**: CORS ошибки в браузере

**Решение**:
- Убедитесь, что `ALLOWED_ORIGINS` в Render содержит ваш Vercel URL
- Формат: `https://ваш-проект.vercel.app` (без слеша в конце)
- Перезапустите сервис в Render после изменения переменных

---

## 📝 Чеклист деплоя

- [ ] Код запушен в GitHub ✅
- [ ] Render: Проверены переменные окружения
- [ ] Render: Добавлен `ALLOWED_ORIGINS` с Vercel URL
- [ ] Render: Запущен деплой
- [ ] Vercel: Проверена переменная `VITE_API_URL`
- [ ] Vercel: Запущен деплой
- [ ] Проверен health check бэкенда
- [ ] Проверена работа фронтенда
- [ ] Проверена валидация форм
- [ ] Проверен онбординг для новых пользователей

---

## 🎉 Готово!

После выполнения всех шагов ваше приложение будет обновлено на Render и Vercel!

Если возникнут проблемы, проверьте логи в Render и Vercel Dashboard.
